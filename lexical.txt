#include <stdio.h>
int main()
{
  int first_num, secondNumber, result;
  first_num = 10;
  secondNumber = 20;
  if (first_num > secondNumber){
    result = first_num - secondNumber;
  }
  else {
  result =  secondNumber - first_num;
  }
}