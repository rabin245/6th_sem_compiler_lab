#include <stdio.h>
int main()
{
  int x, y, z;
  // take input from user
  printf("Enter values of x and y:\n");
  scanf("%d %d", &x, &y);
  // product
  z = x * y;
  /* this is a multi line 
  // testing single line coment inside multi line comment
  enf of multi line comment */
  /* single line testing again */
  /* again multi line cmnt
  klasjdf
  aklsdjflk
  // 
  // sdfjklsklf
  // output
  printf("\nz = %d * %d = %d", x, y, z);
}